<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!--
      Sintassi QWeb inline `{{ }}` e `t-out`, non più Jinja `${ }`: Odoo ha
      cambiato il motore dei modelli di email in 16.0.

      Sul database del cliente il record era già stato convertito
      dall'aggiornamento di Odoo, e `noupdate="1"` lo ha protetto — ma il sorgente
      era rimasto a `${ }`, quindi su un'installazione nuova le email uscivano con
      i segnaposto scritti a lettere. Il testo qui sotto è quello convertito.
    -->
    <record id="email_template_purchase_requisition" model="mail.template">
        <field name="name">Purchase Contract: Send by email</field>
        <field name="model_id" ref="purchase_requisition.model_purchase_requisition"/>
        <field name="subject">Purchase contract {{ object.name or 'n/a' }} ready for review</field>
        <field name="email_from">{{ (object.user_id.email_formatted or user.email_formatted) }}</field>
        <field name="partner_to">{{ object.vendor_id.id }}</field>
        <field name="body_html" type="html">
<div>
Dear <t t-out="object.vendor_id.name"/>,
<br/>
<br/>
Your purchase contract <strong><t t-out="object.name"/></strong> is ready for review.
<br/>
<br/>
Do not hesitate to contact us if you have any questions.
<br/>
</div>
        </field>
        <field name="lang">{{ object.vendor_id.lang }}</field>
        <field name="auto_delete" eval="True"/>
    </record>
</odoo>
